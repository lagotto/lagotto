function updateSource() {
  $.ajax({
    url: "/admin/sources/<%= name %>",
    cache: false
  }).done(function(html){
    $("#source-show").html(html);
    window.setTimeout(updateSource, 5000);
  });
}

$("#source-show").load(function() {
  updateSource();
});