function update() {
  $.ajax({
    url: "/admin/sources/<%= name %>",
    cache: false
  }).done(function(html){
    $("#show").html(html);
    window.setTimeout(update, 5000);
  });
}

$(document).ready(function() {
  update();
});
$(document).load(function() {
  update();
});