The following sources have not been updated for 24 hours as of <%= Time.zone.now.to_formatted_s(:long) %> UTC:

<% unless @sources.empty? -%>
<% @sources.each do |source| -%>
<%= source.display_name %>
<% end -%>
<% else -%>
No stale source found.
<% end -%>

Go to admin dashboard: <%= admin_alerts_url(:host => CONFIG[:hostname], :class => "SourceNotUpdatedError") %>