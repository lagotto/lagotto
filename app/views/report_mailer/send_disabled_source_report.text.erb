The following source has been disabled at <%= Time.zone.now.to_formatted_s(:long) %> UTC:

<%= @source.display_name %>

Go to admin dashboard: <%= source_url(@source.name, :host => CONFIG[:hostname]) %>
