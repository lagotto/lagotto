defaults: &defaults
  source:
    job_batch_size: 200
    max_job_batch_size: 1000
    default_job_batch_size: 200
    staleness: 7.days
    batch_time_interval: 1.hour
<% if node[:app][:seed_additional_sources] %>
  nature:
    requests_per_day: 5000
<% end %>

development:
  <<: *defaults

test:
  <<: *defaults

stage:
  <<: *defaults

production:
  <<: *defaults